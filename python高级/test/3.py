# 有一个数x，我们定义p(x)为x各数字的乘积。
# 然后我们组成一个数列，x, p(x), p(p(x))...然后我们定义x的持久度为数列中第一个个位数所在位置（从零开始）。
# 例如，当x = 99时，该数列为99，9 * 9 = 81, 8 * 1 = 8。所以，99的持久度为2.给定你数字x，请返回其持久度。
#
# 参数： x
# 整数
# 返回值： 返回n的持久度


class Solution(object):
    def endurance(self, x):
        """ 在这里编写你的程序 """
        geweishu =


if __name__ == '__main__':
    x = 128
    result = Solution()
    answer = result.endurance(x)
    print(answer)
